naftiko: 1.0.0-alpha2
info:
  label: Google Cloud Deploy API — Releases
  description: 'Google Cloud Deploy API — Releases. 2 operations. Lead operation: Google Cloud Deploy List releases. Self-contained
    Naftiko capability covering one Google Cloud Deploy business surface.'
  tags:
  - Google Cloud Deploy
  - Releases
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GOOGLE_CLOUD_DEPLOY_API_KEY: GOOGLE_CLOUD_DEPLOY_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloud-deploy-releases
    baseUri: https://clouddeploy.googleapis.com/v1
    description: Google Cloud Deploy API — Releases business capability. Self-contained, no shared references.
    resources:
    - name: projects-projectId-locations-location-deliveryPipelines-pipelineId-releases
      path: /projects/{projectId}/locations/{location}/deliveryPipelines/{pipelineId}/releases
      operations:
      - name: listreleases
        method: GET
        description: Google Cloud Deploy List releases
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createrelease
        method: POST
        description: Google Cloud Deploy Create a release
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: releaseId
          in: query
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GOOGLE_CLOUD_DEPLOY_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloud-deploy-releases-rest
    port: 8080
    description: REST adapter for Google Cloud Deploy API — Releases. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/projects/{projectid}/locations/{location}/deliverypipelines/{pipelineid}/releases
      name: projects-projectid-locations-location-deliverypipelines-pipelineid-releases
      description: REST surface for projects-projectId-locations-location-deliveryPipelines-pipelineId-releases.
      operations:
      - method: GET
        name: listreleases
        description: Google Cloud Deploy List releases
        call: cloud-deploy-releases.listreleases
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createrelease
        description: Google Cloud Deploy Create a release
        call: cloud-deploy-releases.createrelease
        with:
          releaseId: rest.releaseId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloud-deploy-releases-mcp
    port: 9090
    transport: http
    description: MCP adapter for Google Cloud Deploy API — Releases. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: google-cloud-deploy-list-releases
      description: Google Cloud Deploy List releases
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloud-deploy-releases.listreleases
      outputParameters:
      - type: object
        mapping: $.
    - name: google-cloud-deploy-create-release
      description: Google Cloud Deploy Create a release
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: cloud-deploy-releases.createrelease
      with:
        releaseId: tools.releaseId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
